# Exclude Obsidian config
  .obsidian/

  # Exclude everything by default
  *

  # Then explicitly include markdown and PDFs
  !*.md
  !*.pdf
  !.gitignore

  # Include files in subdirectories
  !*/
  !*/*.md
  !*/*.pdf
  !*/*/*.md
  !*/*/*.pdf
